<div class="rules-drop-container">
    <div class="rules-drop-zone" data-id="{{dropId}}">
        {{#if dropTooltip}}
            <i class="fa-solid fa-fw fa-info-circle" data-tooltip="{{localize dropTooltip}}"></i>
        {{/if}}
        <span>{{localize dropText}}</span>
    </div>
</div>
