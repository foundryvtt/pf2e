{{#if members}}
    <ol class="box-list inventory-members">
        {{#unless restricted}}
            <li class="box summary">
                <header>{{localize "PF2E.Actor.Party.Total"}}</header>
                <div class="summary-data">
                    <div>
                        <label><i class="fa-solid fa-coins fa-fw"></i> {{localize "PF2E.CurrencyLabel"}}</label>
                        <span class="value">{{inventorySummary.totalCurrency}}</span>
                    </div>
                    <div>
                        <label><i class="fa-solid fa-scale-unbalanced fa-fw"></i> {{localize "PF2E.Actor.Party.Wealth"}}</label>
                        <span class="value">{{inventorySummary.totalWealth}}</span>
                    </div>
                </div>
                <footer>
                    <i class="fa-solid fa-weight-scale-hanging"></i> {{localize "PF2E.Item.Physical.Bulk.Label"}} {{inventorySummary.totalBulk}}
                </footer>
            </li>
        {{/unless}}
        {{#each members as |member|}}
            <li class="box {{#unless member.limited}}readonly{{/unless}}">
                <div class="actor-link content" data-actor-uuid="{{member.actor.uuid}}" data-action="open-sheet" data-tab="inventory">
                    <img src="{{member.actor.img}}" />
                    <div class="sub-data">
                        <div class="name">{{member.actor.name}}</div>
                        {{#unless member.restricted}}
                            <div class="value">
                                <i class="fa-solid fa-coins fa-fw"></i>
                                {{member.currency}}
                            </div>
                            <div class="value">
                                <i class="fa-solid fa-scale-unbalanced fa-fw"></i>
                                {{member.wealth}}
                            </div>
                        {{/unless}}
                    </div>
                </div>
                {{#unless member.restricted}}
                    <footer>
                        <i class="fa-solid fa-weight-scale-hanging"></i> {{localize "PF2E.Item.Physical.Bulk.Label"}} {{member.actor.inventory.bulk.value}}
                    </footer>
                {{/unless}}
            </li>
        {{/each}}
    </ol>
{{else}}
    {{localize "PF2E.Actor.Party.BlankSlate"}}
{{/if}}
