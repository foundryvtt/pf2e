<ul>
    {{#each tabs as |tab|}}
        <li class="item">
            <label for="enable-tab-{{tab.name}}">{{localize tab.label}}</label>
            <input id="enable-tab-{{tab.name}}" type="checkbox" data-tab-name="{{tab.name}}" {{checked tab.checked}} />
        </li>
    {{/each}}
</ul>
