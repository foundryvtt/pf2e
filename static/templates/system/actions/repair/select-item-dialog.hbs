<article>
    <section class="drop-item-zone">
        {{#if item}}
            <img src="{{item.img}}" class="item-icon" />
            <span>{{item.name}}</span>
        {{else}}
            <img src="systems/pf2e/icons/actions/repair/unknown-item.webp" class="item-icon" />
            <span>{{localize "PF2E.Actions.Repair.SelectItemDialog.DropItemZoneLabel"}}</span>
        {{/if}}
    </section>
    <section class="button-panel">
        <button type="button" data-event-handler="repair" {{disabled (not item)}}>
            <i class="fa-solid fa-fw fa-hammer"></i> {{localize "PF2E.Actions.Repair.SelectItemDialog.RepairButtonLabel"}}
        </button>
        <button type="button" data-event-handler="cancel">
            <i class="fa-solid fa-fw fa-times"></i> {{localize "PF2E.Actions.Repair.SelectItemDialog.CancelButtonLabel"}}
        </button>
    </section>
</article>
